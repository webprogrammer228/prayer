import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
  },
  form: {
    padding: 10,
    textAlign: 'center',
  },
  formHeader: {
    textAlign: 'center',
    fontSize: 24,
    margin: 12,
  },
  formSubHeader: {
    textAlign: 'center',
    fontSize: 16,
    padding: 10,
  },
  warning: {
    marginLeft: 10,
    marginBottom: 10,
    color: 'red',
  },
});
